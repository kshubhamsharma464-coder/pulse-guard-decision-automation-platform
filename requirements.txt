fastapi>=0.115
uvicorn>=0.30
pydantic>=2.0
pydantic-settings>=2.4
pytest>=8.0
httpx>=0.27
sqlalchemy>=2.0
alembic>=1.13
psycopg2-binary>=2.9
python-dotenv>=1.0
pyjwt>=2.8

# -- Phase 5: distributed policy cache platform --
redis>=5.0                 # Settings.cache_backend="redis" -- optional, off by default
prometheus-client>=0.20    # GET /api/v1/metrics
fakeredis>=2.20             # test-only: simulates Redis (and Redis failures) with no live server
